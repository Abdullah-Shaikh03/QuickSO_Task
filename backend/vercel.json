{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",   
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && tsc --noEmit",
  "env": {
    "DATABASE_URL": "your-postgres-url"
  }
}
